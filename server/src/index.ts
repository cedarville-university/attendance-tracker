// Placeholder for the Fastify server added in Phase 1 of the Canvas LTI
// migration plan (docs/canvas-lti/progress.md). Exists now only so
// tsconfig.json has a real input to typecheck.
export {};
